========= GIT ESSENTIALS =======

1. Git essentials
    - Git is a version control system used for tracking changes in computer files. 
    - Distributed, efficency, 


git server



repository 
remote repository
local repository

file status in git
unstracked 
unmodified 
modified 
staged 

git set up

git commands

git init
git clone url 
git add <file-name> 
git add . // thêm tất cả vào vùng theo dõi
git commit -m
git push -u: đâỷ lên lần đầu tiên
git push: các lần tiếp theo 
git pull: 

git config --global user.name "dominicotuan"
git config --global use.email "dquangtuan07@gmail.com"


git init
git status

=============================== GIT BRANCH =====================

git nhánh là gì? 
    - git branch: là cáCH dùng để phân nhánh và ghi lại lịch sử 
    không ảnh hưởng đến các brach khác nên không có thể thục hiện nhiều thay đổi đô g
    đồng thời trong một repository
    git brach đã được phân nhánh có thể chaỵh song song với nahu mà  không ảnh hưởng gì tới nhau

    khi các nhánh cần tông hợp lịa với nhau ta chỉ cần hợp lại một nhánh khác

    Master brach, your team brach, your branch 
    



